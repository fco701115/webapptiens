version: '3.8'

services:
  app:
    build: .
    container_name: webapptiens
    restart: always
    ports:
      - "3002:3002"
    environment:
      - NODE_ENV=production
      - PORT=3002
      - DB_HOST=webapptiens-app-napiu2
      - DB_NAME=db-webapptiens
      - DB_USER=postgres
      - DB_PASSWORD=Rg9svf6GK22MJY5aLwsf
      - DB_PORT=5432
    networks:
      - webapptiens-net

networks:
  webapptiens-net:
    driver: bridge
